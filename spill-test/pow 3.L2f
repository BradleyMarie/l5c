(((edi <- 0)

  :compare
  (cjump edi < 5 :funcCall :exit)

  :funcCall
  (eax <- edi)
  (call :pow3)
  (eax <<= 1)
  (eax += 1)
  (eax <- (print eax))
  (edi += 1)
  (goto :compare)

  :exit)

 (:pow3
  (product <- 1)
  (count <- eax)

  :comp
  (cjump 0 < count :do :done)

  :do
  (product *= 3)
  (count -= 1)
  (goto :comp)

  :done
  (eax <- product)
  (return)))
